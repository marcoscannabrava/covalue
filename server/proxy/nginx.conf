server {
  listen 80 default_server;
  listen [::]:80 default_server;

  server_name _;

  location / {
    proxy_pass http://localhost:8080/;
  }
}

server {
  listen 443 ssl;
  server_name _;

  location /  {
    proxy_pass http://localhost:8080/;
  }
}