{
  "name": "covalue",
  "version": "1.0.0",
  "description": "Startup scripts for Heroku Deployment. 1) Build -> 2) Start.",
  "engines": {
    "node": "12.16.x",
    "yarn": "1.22.x"
  },
  "scripts": {
    "build": "cd client && yarn && yarn build && cd ../server && yarn && cd ..",
    "start": "node server/app.js"
  }
}